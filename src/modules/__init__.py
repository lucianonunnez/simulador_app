"""Orquestadores de pantalla.

Cada módulo (`module1`, `module2`, `module3`) expone una función `render()`
que el router de `streamlit_app.py` invoca según la navegación elegida.
Conecta la capa `core` (datos/cálculo) con la capa `ui` (presentación).
"""
