streamlit==1.38.0
streamlit-authenticator==0.3.2
bcrypt>=4.0.0
duckdb>=1.0.0
# Conector a Supabase/PostgreSQL (fuente de datos remota). Si no hay config de
# Supabase en secrets, la app usa DuckDB local y este driver no se importa.
psycopg2-binary>=2.9.9
# pandas>=2.2 es requerido: el feature engineering usa
# groupby(...).apply(..., include_groups=False), que solo existe desde 2.2.
pandas>=2.2,<3
numpy>=1.24.0
openpyxl>=3.1.5
plotly>=5.18.0
requests>=2.31.0
scikit-learn==1.6.1
lightgbm>=4.0.0
# tensorflow-cpu se movió a requirements-nn.txt: es OPCIONAL (solo habilita la
# comparativa con la red neuronal del Módulo 3) y cuesta ~950MB de disco
# instalado más ~500MB de RAM al importar (medido con TF 2.16 cpu). Sin él,
# el módulo corre en modo LightGBM.
joblib>=1.3.0
